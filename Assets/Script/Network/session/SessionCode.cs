using System;

namespace Lorance.RxScoket.Session
{
	public class SessionCode
	{
//		public static byte[] EnCode(byte protoGroup, String msg) {
//			var msgBytes = Common.ToBigEnd(BitConverter.GetBytes (msg));
//			var msgLength = msgBytes.Length;
//			var lengthBytes = Common.ToBigEnd(BitConverter.GetBytes (length));
//			var rst = new byte[1 + msgLength + lengthBytes.Length];
//			rst [0] = protoGroup;
//			Common.concat ();
////			return rst.
//		}
	}
}

